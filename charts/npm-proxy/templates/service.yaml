apiVersion: v1
kind: Service
metadata: 
  name: npm-proxy-service
  
spec: 
  selector: 
    app: npm-proxy-deployment
    
  type: ClusterIP
  ports: 
    
    - name: npm-registry-5000
      protocol: TCP
      port: 5000
      targetPort: 5000
